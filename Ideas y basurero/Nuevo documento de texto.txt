Comandos o ideas generales que se puedan llegar a implementar

Este comando permite ocultar un dataframe 

if st.checkbox('Show dataframe'):
    chart_data = pd.DataFrame(
       np.random.randn(20, 3),
       columns=['a', 'b', 'c'])

    chart_data

